##1.利用datetime模块，批量生成11月份每天的txt文件import datetimestart_time = datetime.datetime(2021,11,1)end_time = datetime.datetime(2021,11,30)#days 去得到天数os.mkdir('日期文件')days_time = (end_time - start_time).days +1#用变量代替常量for  i in range(days_time):     doc = start_time.strftime('%Y-%m-%d')+'.txt'     f = open(f'日期文件\{doc}','w')     f.write(doc)     start_time += datetime.timedelta(days=1)     f.close()                    ##2.生成上面的文件之后，再一次在每个文件中写入文件名##3.将上面生成的所有文件名之后加上‘_NEW’from datetime import datetime  as dtimport osfor i in range(1,31):     date =  dt.strftime(dt.now(),'%Y')     filename = './'+date+'-11-'+str(i)+'.txt'     newname='./'+date+'-11-'+str(i)+'_new'+'.txt'     with open(filename,'w') as f:          f.write(date+str(i))          f.close()          os.rename(filename,newname) ##4.假设有一个英文文本文件，编写一个程序读取其内容，##并将里面的大写字母变成小写字母，小写字母变成大写字母str1 =  'AbCdEfG'str2 = str1.swapcase()print(str2)f = open('test.txt','r+')str1 = f.readstr2 = str1.swapcase()f.seek(0)f.write(str2)f.close()